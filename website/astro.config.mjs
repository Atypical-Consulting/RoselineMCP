// @ts-check
import { defineConfig } from 'astro/config';

// GitHub Pages project site: https://atypical-consulting.github.io/RoselineMCP
export default defineConfig({
  site: 'https://atypical-consulting.github.io',
  base: '/RoselineMCP/',
  trailingSlash: 'ignore',
});
